'use strict';

const helpers = require('./helpers-2f1a8ddc.js');

/**
 * Convert a {@link KeyboardEvent} into a {@link KeyboardShortcut}
 * @param event - KeyboardEvent
 * @returns The corresponding {@link KeyboardShortcut}
 */
function keyboardShortcutFromKeyboardEvent(event) {
    const { key } = event;
    if (event.shiftKey) {
        switch (key) {
            case ' ':
                return 'MAJ+SPACE';
            case 'Tab':
                return 'MAJ+TAB';
        }
    }
    else if (event.altKey) {
        switch (key) {
            case 'ArrowDown':
                return 'ALT+DOWN_ARROW';
            case 'ArrowUp':
                return 'ALT+UP_ARROW';
        }
    }
    else if (event.ctrlKey) {
        if (helpers.isHomeKey(event)) {
            return 'CTRL+HOME';
        }
        else if (helpers.isEndKey(event)) {
            return 'CTRL+END';
        }
        else if (key === 'a') {
            return 'CTRL+A';
        }
    }
    else {
        switch (key) {
            case 'ArrowDown':
                return 'DOWN_ARROW';
            case 'ArrowUp':
                return 'UP_ARROW';
            case 'ArrowLeft':
                return 'LEFT_ARROW';
            case 'ArrowRight':
                return 'RIGHT_ARROW';
        }
        if (helpers.isHomeKey(event)) {
            return 'HOME';
        }
        else if (helpers.isEndKey(event)) {
            return 'END';
        }
        else if (helpers.isPageUpKey(event)) {
            return 'PAGE_UP';
        }
        else if (helpers.isPageDownKey(event)) {
            return 'PAGE_DOWN';
        }
        else if (helpers.isEnterKey(event)) {
            return 'ENTER';
        }
        else if (helpers.isSpaceKey(event)) {
            return 'SPACE';
        }
        else if (helpers.isTabKey(event)) {
            return 'TAB';
        }
        else if (helpers.isEscapeKey(event)) {
            return 'ESCAPE';
        }
    }
    return 'UNKNOWN';
}

exports.keyboardShortcutFromKeyboardEvent = keyboardShortcutFromKeyboardEvent;

//# sourceMappingURL=keyboard-event-40ecdc27.js.map