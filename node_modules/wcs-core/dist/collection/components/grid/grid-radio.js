import { h } from "@stencil/core";
export const GridRadio = ({ checked, row, rowIndex, totalRows, onClick }) => {
    let input;
    return (h("div", { class: `grid-radio ${checked ? 'checked' : ''}`, onClick: _ => input.click() }, h("input", { type: "radio", name: "selection", "aria-setsize": totalRows, "aria-posinset": rowIndex, tabindex: "-1", checked: checked, "aria-checked": `${checked}`, ref: (el) => (input = el), onClick: _ => onClick(row) })));
};
//# sourceMappingURL=grid-radio.js.map
